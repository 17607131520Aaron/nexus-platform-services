server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://staging-mysql-host:3306/nexus_staging?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: staging_user
    password: staging_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
